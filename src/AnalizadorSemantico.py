txt = " "
cont = 0
def incrementarContador():
	global cont
	cont +=1
	return "%d" %cont

class Nodo():
	pass

class programa(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id
			#p[0] = program(p[1],"program")

class variable(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class variable1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class variable1_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones2(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones3(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones4(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones5(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones6(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones7(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones8(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones9(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones10(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones11(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones12(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones13(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresiones14(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class expresionesEpsilon(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class cicloFor(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class cicloWhen(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class cicloKeep(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class comparacion(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class asignar(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class actualizar(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class actualizar_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class actualizar1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class cambiar_direccion(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class direccion_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class direccion_2(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class direccion_3(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class direccion_4(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class colocar(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class colocar1_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class colocar1_2(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class elevar(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class elevar1_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class elevarr1_2(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class encender(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class mover(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class pos_inicio(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class llamar(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class procedimientos_1(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class procedimientos_2(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class epsilon(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

class error(Nodo):
	def __init__(self, name):
		self.name = name
	
	def imprimir(self, ident):


		print (ident + "Nodo: "+self.name)		

	def traducir(self):
		global txt
		id = incrementarContador()

		return id

